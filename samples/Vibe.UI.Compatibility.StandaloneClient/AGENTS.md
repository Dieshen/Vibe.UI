# Standalone Client Compatibility Fixture

This sample is a Vibe.UI compatibility fixture for a standalone Blazor WebAssembly app.

- Keep the app minimal and focused on package references, static web assets, DI registration, and client-side component interactivity.
- Use project references to the local Vibe.UI source projects instead of package references.
- Do not add product documentation or broad component demos here; use `samples/Vibe.UI.Docs` for that.
