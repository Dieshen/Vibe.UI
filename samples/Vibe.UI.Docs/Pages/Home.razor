@page "/"

<PageTitle>Vibe.UI - Modern Blazor Component Library</PageTitle>

<div class="landing-page">
    <!-- Hero Section -->
    <section class="hero">
        <h1 class="hero-title">Vibe.UI</h1>
        <p class="hero-subtitle">
            Modern Blazor component library with 93+ production-ready components
        </p>
        <div class="hero-actions">
            <a href="@GetUrl("getting-started")" class="btn btn-primary">Get Started</a>
            <a href="@GetUrl("components/button")" class="btn btn-secondary">Browse Components</a>
        </div>
    </section>

    <!-- Quick Start Section -->
    <section class="section">
        <h2>Quick Start</h2>
        <p>Install the CLI tool and start adding components to your Blazor project:</p>
        <CodeBlock Code="@QuickStartCode" Language="bash" />
    </section>

    <!-- Features Grid -->
    <section class="section">
        <h2>Features</h2>
        <div class="features-grid">
            <div class="feature-card">
                <h3>🎨 93+ Components</h3>
                <p>Comprehensive collection of production-ready components for modern web applications</p>
            </div>
            <div class="feature-card">
                <h3>🛠️ CLI Tool</h3>
                <p>shadcn/ui style workflow - copy components to your project for full control</p>
            </div>
            <div class="feature-card">
                <h3>🎭 Theming System</h3>
                <p>Built-in dark mode support with customizable themes</p>
            </div>
            <div class="feature-card">
                <h3>♿ Accessible</h3>
                <p>WCAG AA compliant with proper ARIA attributes and keyboard navigation</p>
            </div>
            <div class="feature-card">
                <h3>📱 Responsive</h3>
                <p>Mobile-first design that works on all screen sizes</p>
            </div>
            <div class="feature-card">
                <h3>⚡ Performance</h3>
                <p>Optimized for speed with minimal bundle size</p>
            </div>
        </div>
    </section>

    <!-- Popular Components Showcase -->
    <section class="section">
        <h2>Popular Components</h2>
        <div class="component-showcase">
            <a href="@GetUrl("components/button")" class="showcase-item">Button</a>
            <a href="@GetUrl("components/card")" class="showcase-item">Card</a>
            <a href="@GetUrl("components/input")" class="showcase-item">Input</a>
            <a href="@GetUrl("components/modal")" class="showcase-item">Modal</a>
            <a href="@GetUrl("components/datepicker")" class="showcase-item">DatePicker</a>
            <a href="@GetUrl("components/chart")" class="showcase-item">Chart</a>
        </div>
    </section>

    <!-- Installation Options -->
    <section class="section">
        <h2>Installation Options</h2>
        <div class="installation-options">
            <div class="option-card">
                <h3>CLI (Recommended)</h3>
                <p>Full source code control - customize any component</p>
                <CodeBlock Code="@CliInstallCode" Language="bash" />
            </div>
            <div class="option-card">
                <h3>NuGet Package</h3>
                <p>Traditional package reference for quick setup</p>
                <CodeBlock Code="@PackageInstallCode" Language="bash" />
            </div>
        </div>
    </section>
</div>

@code {
    [Inject]
    private NavigationManager Navigation { get; set; } = default!;

    private string QuickStartCode => @"# Install CLI tool
dotnet tool install -g Vibe.UI.CLI

# Initialize in your project
cd MyBlazorApp
vibe init

# Add components
vibe add button
vibe add card
vibe add input";

    private string CliInstallCode => @"dotnet tool install -g Vibe.UI.CLI
vibe init
vibe add button";

    private string PackageInstallCode => @"dotnet add package Vibe.UI";

    private string GetUrl(string path)
    {
        return $"{Navigation.BaseUri}{path}";
    }
}
